require 'spec_helper'

describe ImageUpload do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ImageUpload.create!(@valid_attributes)
  end
end
